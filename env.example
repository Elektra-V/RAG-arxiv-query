# ============================================================================
# RAG API - Environment Configuration
# ============================================================================
# Copy this to .env and uncomment ONE configuration section below

# ============================================================================
# OPTION 1: Company API (Production)
# ============================================================================
# Configure your company API gateway that provides access to open source models
# Works exactly like OpenRouter - just change the values below!
LLM_PROVIDER="openai"
EMBEDDING_PROVIDER="openai"  # or "huggingface" for free embeddings
OPENAI_BASE_URL="https://genai.iais.fraunhofer.de/api/v2"  # Your company API gateway URL
OPENAI_AUTH_USERNAME="your-username"  # Basic auth username (if required)
OPENAI_AUTH_PASSWORD="your-password"  # Basic auth password (if required)
OPENAI_API_KEY="xxxx"  # Usually "xxxx" or your actual API key
OPENAI_MODEL="Llama-3-SauerkrautLM"  # Model name from your company API
OPENAI_EMBEDDING_MODEL="text-embedding-3-small"  # Embedding model (if using OpenAI embeddings)

# Optional: Custom headers for company APIs (if your gateway requires additional headers)
# Format: "Header-Name:value" separated by commas
# Example: "X-Custom-Header:value1,Another-Header:value2"
COMPANY_API_EXTRA_HEADERS=""  # Leave empty if not needed

# ============================================================================
# OPTION 2: Local Testing with Free APIs
# ============================================================================
# Uncomment this section and comment Option 1 above

# LLM_PROVIDER="openai"
# EMBEDDING_PROVIDER="huggingface"  # FREE - no API key needed!
# OPENAI_BASE_URL="https://api.together.xyz/v1"  # Get key at https://together.ai/
# OPENAI_API_KEY="your-together-api-key-here"
# OPENAI_MODEL="meta-llama/Llama-3-8b-chat-hf"
# HUGGINGFACE_MODEL="sentence-transformers/all-MiniLM-L6-v2"
# OPENAI_AUTH_USERNAME=""
# OPENAI_AUTH_PASSWORD=""

# ============================================================================
# OPTION 3: Test Ingestion Only (No LLM API Key)
# ============================================================================
# Uncomment this to test ingestion without LLM
# Useful for verifying the pipeline works before getting an API key

# LLM_PROVIDER="openai"
# EMBEDDING_PROVIDER="huggingface"  # FREE!
# OPENAI_BASE_URL=""
# OPENAI_API_KEY=""
# OPENAI_MODEL="gpt-4o-mini"  # Not used for ingestion
# HUGGINGFACE_MODEL="sentence-transformers/all-MiniLM-L6-v2"
# OPENAI_AUTH_USERNAME=""
# OPENAI_AUTH_PASSWORD=""

# ============================================================================
# COMMON SETTINGS (All Configurations)
# ============================================================================
QDRANT_URL="http://localhost:6334"
QDRANT_COLLECTION="arxiv_papers"
ARXIV_QUERY="quantum computing"
ARXIV_MAX_DOCS=5

# Optional: LangSmith for tracing (leave empty if not using)
LANGSMITH_API_KEY=""
LANGSMITH_TRACING=false
LANGSMITH_PROJECT="rag-api-langchain"
LANGSMITH_ENDPOINT="https://api.smith.langchain.com"

DUCKDUCKGO_RESULTS=3

# Ports (changed to 9000+ range to avoid conflicts)
LANGCHAIN_HOST="0.0.0.0"
LANGCHAIN_PORT=9010
LLAMAINDEX_HOST="0.0.0.0"
LLAMAINDEX_PORT=9020
INGESTION_HOST="0.0.0.0"
INGESTION_PORT=9030

# OpenRouter optional headers (for better rankings on openrouter.ai)
# These are optional but recommended
OPENROUTER_HTTP_REFERER=""  # Optional: Your site URL (e.g., "https://your-site.com")
OPENROUTER_X_TITLE=""        # Optional: Your site name (e.g., "My RAG App")

# Company API custom headers (for any gateway requiring additional headers)
# Format: "Header-Name:value" separated by commas
# Example: "X-Custom-Header:value1,Another-Header:value2"
# Leave empty if not needed
COMPANY_API_EXTRA_HEADERS=""

